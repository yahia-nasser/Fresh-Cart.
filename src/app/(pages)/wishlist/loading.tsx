import React from "react";

const Loading = () => {
  return (
    <div className="mx-auto flex justify-center items-center h-screen">
      <i className="fas fa-spinner fa-spin text-9xl"></i>
    </div>
  );
};

export default Loading;
